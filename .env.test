# Committed to git. Loaded only when NODE_ENV/--mode is "test" (Playwright's webServer processes), overriding .env's real local Supabase values back to empty so e2e keeps exercising the dev-JWT auth fallback (decisions.md D23).
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
DATABASE_URL=
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
