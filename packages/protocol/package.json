{
  "name": "@new-mj/protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "test": "node --test",
    "verify": "pnpm typecheck && pnpm lint && pnpm test"
  }
}
