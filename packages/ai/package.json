{
  "name": "@new-mj/ai",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "scripts": {
    "build": "tsup --config tsup.config.ts",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint src test",
    "test": "vitest run --tags-filter '!slow'",
    "test:full": "vitest run",
    "bench:two-ply": "node src/junk/two-ply-benchmark-cli.ts",
    "generate:two-ply-baseline": "node src/junk/two-ply-baseline-cli.ts",
    "compare:two-ply-baseline": "node src/junk/two-ply-baseline-ab-cli.ts",
    "bench:two-ply-structured": "node src/junk/two-ply-structured-cli.ts",
    "search:two-ply-adversarial": "node src/junk/two-ply-adversarial-search-cli.ts",
    "verify": "pnpm typecheck && pnpm lint && pnpm test && pnpm build",
    "verify:full": "pnpm typecheck && pnpm lint && pnpm test:full && pnpm build"
  },
  "dependencies": {
    "@new-mj/core": "workspace:*"
  }
}
