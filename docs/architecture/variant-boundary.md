# 公共 vs 玩法专属：边界判定准则与台账

> 把“什么该放公共层、什么该留在玩法自己手里”显式化、可追溯，而不是每次凭感觉判断。当前有 junk、bloodbattle、hangzhou 三个玩法；它们共享形状不自动证明某机制是通用真理。

## 1. 谁会用到、什么时候查

- 加新玩法前：查台账，判断哪些能力可以直接复用公共契约，哪些必须自己实现一份。
- 想把某段"重复代码"提取成公共实现前：先查这里，看是否达到转正条件——达不到就不提取，继续复制。
- 阶段收尾复盘时：更新台账的"已验证玩法数"列，评估是否有条目该转正。

## 2. 判定准则

1. **验证玩法数门槛**：一个机制只有被 ≥2 个**已经真实实现、且彼此有实质差异**的玩法验证过，才有资格进入"已确认公共"。当前垃圾胡和血战都还比较简单（都是国标模式：4 面子+1 对，声明窗口都是"仅合法响应者进入"），两者都用同一形状不代表这个形状是通用真理。
2. **诱惑测试**（借用 `doc-map.md` 的说法）：想把某段逻辑提取成公共实现时，问自己"下一个明显不同的玩法（比如日麻）大概率也会长这样吗"——不确定就不提取，继续复制到各玩法自己的文档/代码里。
3. **默认保守**：凡是跟"计分""排名""庄家""听牌判定"这类麻将规则本身沾边的机制，即使今天两个玩法实现完全一样，也默认当作"私有，暂时雷同"处理，只在契约层（`engine-contract.md`）留一个方法签名当扩展点，不把具体公式放进公共实现里。只有纯编排/纯传输层面的东西（连接鉴权、房间生命周期状态机骨架、事件信封格式）才默认公共。
4. **转正流程**：满足门槛后，把实现从各 `variants/*.md`/代码里挪到公共契约文档/代码，并在本文件 §4 记一笔（架构级的额外写进对应 `architecture/*.md` 段落）。转正是单向操作，不追求预判。

## 3. 边界台账

| 机制                                         | 现状                                                  | 已验证玩法数        | 当前实现是否一致                                                                    | 转正条件                           | 备注                                                                                                                                           |
| -------------------------------------------- | ----------------------------------------------------- | ------------------- | ----------------------------------------------------------------------------------- | ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| 连接鉴权（握手 JWT）                         | 公共                                                  | 3/3                 | 是                                                                                  | 已达标                             | 与麻将规则无关                                                                                                                                 |
| 房间生命周期骨架（waiting/in-game/finished） | 公共                                                  | 3/3                 | 是                                                                                  | 已达标                             | 纯编排，见 `session-mechanics.md`                                                                                                              |
| 事件信封（seq/visibility）                   | 公共                                                  | 3/3                 | 是                                                                                  | 已达标                             | 见 `engine-contract.md`                                                                                                                        |
| engine-api 四签名                            | 公共                                                  | 3/3                 | 是                                                                                  | 已达标                             | 见 `engine-contract.md`                                                                                                                        |
| PlayerViewBase 最小骨架                      | 公共                                                  | 3/3                 | 是                                                                                  | 已达标                             | 玩法可各自扩展，见 `engine-contract.md`                                                                                                        |
| 声明窗口机制（仅合法响应者进入）             | 公共（机制层面）                                      | 3/3                 | 是（机制一致，优先级顺序不同）                                                      | 已达标（机制部分）                 | 优先级表本身是私有的，见各 `variants/*.md`                                                                                                     |
| 跨局座位连续次数计数（`dealerStreak`）       | **公共**（纯编排，Room 层通用实现）                   | 1/3 用到，其余忽略  | 是                                                                                  | 已达标                             | 只做座位号比较，不理解规则；见下一行"庄家轮换公式"与 `session-mechanics.md` §5——这行是"谁连续坐了几次庄"的计数机制本身，不是"该不该连庄"的规则 |
| 首局庄家公式                                 | **私有**（契约层留 dispatch 签名）                    | 1/3 将用到          | 否（Junk v3 按 seed 随机；既有玩法保留座位 0）                                      | 待 v3 实现验证                     | `computeInitialDealer(seed)` 保证可复现；Room 只生成 seed、调用并保存结果，不按 `rulesetId` 分支                                               |
| 庄家轮换**公式**                             | **私有**（契约层留 dispatch 签名）                    | 3/3                 | 否（junk/bloodbattle 顺时针轮转，hangzhou 庄家胡牌或流局才连庄，否则轮庄）          | 已验证——结论：确认私有，不转正     | hangzhou 是本项目第一个连庄玩法，证明了 junk/bloodbattle 的"顺时针"只是巧合，不是通用真理；三个玩法各自维护自己的实现，不提取公共代码          |
| 计分/结算方式                                | 私有                                                  | 3/3                 | 否（垃圾胡固定分，血战按番倍数，杭州番组倍数直接相乘）                              | —                                  | 已经证明确实需要私有，不用等验证                                                                                                               |
| 会话排名策略                                 | **私有**（当前代码在 Room 层实现，尚未拆成 dispatch） | 3/3，均为纯分数排序 | 是，但视为巧合                                                                      | 需要一个排名机制明显不同的玩法验证 | 日麻的 uma/oka（名次奖惩分）是典型反例，一旦接入现有实现需要重新拆分                                                                           |
| 听牌/胡牌合法性判定                          | 私有                                                  | 3/3                 | 否（垃圾胡无起胡限制，血战不含定缺花色+基本型/七对，杭州支持财神代打+三牢点炮限制） | —                                  | 判定逻辑各玩法差异大，不考虑提取                                                                                                               |

## 4. 变更记录（转正与边界调整历史）

- 杭州麻将（第三个玩法）落地：`跨局座位连续次数计数（dealerStreak）`新增为公共机制（纯编排，Room 层通用实现，见 `session-mechanics.md` §5）。`庄家轮换公式`完成验证——hangzhou 的连庄逻辑与 junk/bloodbattle 的顺时针轮转不同，确认结论为**保持私有**，不提取公共代码；台账里"需要连庄玩法验证"的备注已解除。

## 5. 已知的下一次检验点

计划中的下一个玩法是日麻。日麻一旦立项，`会话排名策略`（本台账里唯一还标"待验证"的条目）应该是第一批被重新审视的对象；日麻的 uma/oka 名次奖惩分如果接入，届时按 §2 的流程决定转正还是继续保持私有。`庄家轮换公式`已经被 hangzhou 验证过，不用等日麻。
